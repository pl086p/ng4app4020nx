import {Component, ViewEncapsulation} from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'origin-bootstrap',
    templateUrl: './origin-bootstrap.component.html',
    encapsulation: ViewEncapsulation.None,
})
export class OriginBootstrapComponent { }
